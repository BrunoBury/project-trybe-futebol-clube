// import { Router, Request, Response } from 'express';
// import UserController from '../Controller/userController';

// const userRouter = Router();

// userRouter.post('/users', (req: Request, res: Response) => UserControlles.getAllUsers(req, res));
